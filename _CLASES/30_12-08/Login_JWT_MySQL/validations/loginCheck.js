/* const userModel = require('../models/userModel.js');

if(userModel.findOne() == user){
    req.session.user = user
}
const user = req.body.user;
let usuarioALoguearse = 

/* req.session.usuarioLogueado = usuarioALoguearse; */ 